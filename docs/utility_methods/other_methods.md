# 其他方法

- `reset`：将给定状态（包括子状态）的所有变量（Vars）重置为其默认值。
- `get_value`：返回一个变量（Var）的值，**但不跟踪对该变量的更改**。这对于序列化非常有用，因为跟踪包装器被认为是不可序列化的。
- `dict`：将所有状态变量（及子状态）作为一个字典返回。这内部用于页面首次加载时需要"水合（hydrated）"并发送给客户端。

## 特殊属性

- `dirty_vars`：自上次状态发送给客户端以来已被修改的所有变量名的集合。这在内部用于确定处理事件后哪些变量需要发送给客户端。
